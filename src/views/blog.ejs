<%- include('layout') %>

<h2 data-aos="fade-in">Blog</h2>
<% posts.forEach(post => { %>
  <article data-aos="fade-up">
    <h3><%= post.title %></h3>
    <p><%= post.content %></p>
  </article>
<% }) %>
